interface NewRestaurantFormType {
  name: string;
  address: string;
  region: string;
  dayOpen: string[];
  operatingHours: string[];
  phoneNumber: string;
  website: string;
  cuisineType: string;
  priceRange: number;
  reservationRequired: boolean;
}
